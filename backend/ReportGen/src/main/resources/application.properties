spring.application.name=reportgen
server.port=8084

# Firestore configuration (can be set via env vars or overrides)
firestore.project-id=mobilesec-ms
firestore.credentials-path=C:/Users/hlaks/Documents/MobileSec-MS-feature-user-auth/backend/ReportGen/src/main/resources/mobilesec-ms-firebase-adminsdk-fbsvc-05cadec770.json

# H2 Configuration (unused but kept for legacy compatibility)
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Upload limits (just in case)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
